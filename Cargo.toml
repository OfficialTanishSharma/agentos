[package]
name = "agentos"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Local-first memory MCP server for coding agents"
repository = "https://github.com/OfficialTanishSharma/agentos"

[dependencies]
anyhow = "1.0"
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
